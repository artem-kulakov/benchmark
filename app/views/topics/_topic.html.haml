%tr
  %td= topic.content
  %td
    .author= link_to topic.user.name, topic.user
    .date= time_ago_in_words(topic.created_at) + ' ago'
//%div
//  = topic.content
//  = link_to topic.user.name, topic.user
//  = time_ago_in_words(topic.created_at) + ' ago'
